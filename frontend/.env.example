# Backend API URL (optional). When set, frontend calls this host instead of relative /api.
# Leave unset when using Vite proxy (default: localhost:3001) or when frontend and backend share the same origin.
VITE_API_URL=https://ai-jukebox-backend-production.up.railway.app

# Local dev: point the frontend at a remote backend (optional).
# Leave unset to use Vite proxy to localhost:3001.
VITE_API_URL=https://ai-jukebox-backend-production.up.railway.app

# Production (e.g. Railway): nginx proxies /api/ and /queue to this URL. Required for deployed frontend.
# BACKEND_URL=https://ai-jukebox-backend-production.up.railway.app

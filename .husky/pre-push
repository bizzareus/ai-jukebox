#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "husky: running build before push..."
npm run build
if [ $? -ne 0 ]; then
  echo "husky: build failed. Push aborted. Fix the build and try again."
  exit 1
fi
echo "husky: build passed."
exit 0
